#include <stdio.h>

void reverseWord(char word[]) // 2 pointer approach
{
    
}

int main() 
{
    char name[20];
    printf("Enter a name: ");
    scanf("%[^\n]", name); // [^\n] gets untill the end
    printf("Name is: %s\n", name);
    //puts(name); alternative way to print

}