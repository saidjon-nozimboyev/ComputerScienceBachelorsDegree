#include <stdio.h>

int main()
{
    int num;
    printf("Please enter the number:");
    scanf("%i",&num);
    if (num%2 & 0)
        printf("%i is even",num);
    else
        printf("%i is odd",num);
}
